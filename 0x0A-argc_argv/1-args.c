#include <stdio.h>
/**
 * main - prints number of arguments in a function
 * Description: program execution starts from here
 * @argc: number of args
 * @argv: array of args
 * Return: 0
 */
int main(int argc, char *argv[]__attribute__((unused)))
{
	printf("%d\n", argc);
	return (0);
}

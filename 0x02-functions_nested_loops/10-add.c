#include "main.h"
/**
 * add - adds two integers
 * Description: uses + to add
 * @a: integer argument one
 * @b: integer argument two
 * Return: result
 */
int add(int a, int b)
{
	int res = a + b;

	return (res);
}
